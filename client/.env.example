# Base URL of the backend API. Only needed in development — in production
# the built app is served from the same origin as the API, so no base URL
# override is needed there.
VITE_API_URL=http://localhost:3000
